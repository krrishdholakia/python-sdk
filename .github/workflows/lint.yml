name: lint

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: black
        uses: psf/black@stable
        with:
          options: "--check"
          version: "22.3.0"
      - name: flake8
        uses: py-actions/flake8@v2
